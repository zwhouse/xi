<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Xi - register</title>
</head>
<body>

<h1>register</h1>
<form action="/user/register" method="post">

    <!-- TODO handle error & info -->

    <label for="name">name</label><br>
    <input id="name" type="text" name="name" value="{{#if data.name}}{{data.name}}{{/if}}">
    {{#if errors.name}} ! {{errors.name.msg}}{{/if}}<br>

    <label for="email">email</label><br>
    <input id="email" type="text" name="email" value="{{#if data.email}}{{data.email}}{{/if}}">
    {{#if errors.email}} ! {{errors.email.msg}}{{/if}}<br>

    <label for="password">password</label><br>
    <input id="password" type="password" name="password">
    {{#if errors.password}} ! {{errors.password.msg}}{{/if}}<br>

    <input type="submit" value="register">
</form>
</body>
</html>